"""PyTorch Day 3 -- Exercises. Fill in the TODOs, then run: python3 exercises.py"""
import torch
from torch import nn

# 1. Create a single nn.Linear layer that takes 5 input features and
#    produces 2 output features. Pass a random batch of 3 examples
#    through it (shape (3, 5)) and print the output shape.
# TODO

# 2. Define a class TwoLayerNet(nn.Module) with __init__(self, in_size,
#    hidden_size, out_size) that builds two Linear layers with a ReLU in
#    between (same pattern as SimpleNet in main.py, but sizes are
#    parameters). Create an instance with in_size=10, hidden_size=20,
#    out_size=4, and run a batch of 6 random examples through it.
# TODO

# 3. Using the model from exercise 2, print its total parameter count
#    and the name+shape of every parameter (same pattern as main.py's
#    Parameters section).
# TODO

# 4. Build a 4-layer model using nn.Sequential: Linear(8, 16) -> ReLU ->
#    Linear(16, 16) -> ReLU -> Linear(16, 1). Run a batch of 4 random
#    examples (shape (4, 8)) through it and print the output.
# TODO
