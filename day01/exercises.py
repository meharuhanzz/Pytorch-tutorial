"""PyTorch Day 1 -- Exercises. Fill in the TODOs, then run: python3 exercises.py"""
import torch

# 1. Create a 1D tensor of the numbers 10 to 20 (inclusive) using
#    torch.arange(), then print its shape and dtype.
# TODO

# 2. Create two 3x3 tensors of random numbers. Compute their element-wise
#    product AND their matrix product. Print both, and print the shapes
#    to confirm they're different operations.
# TODO

# 3. Create a tensor `t = torch.arange(24)` and reshape it into a
#    (2, 3, 4) tensor. Print its shape, then index into it to grab the
#    single number at position [1, 2, 3].
# TODO

# 4. Write device-agnostic code: create a `device` variable (cuda if
#    available, else cpu), create a 3x3 random tensor, move it to that
#    device, and print which device it ended up on.
# TODO
